export enum ECurrency {
    TRY = 'TRY',
    AZN = 'AZN',
    USD = 'USD',
}

export enum EAPIProcessStatus {
    IDLE = 'IDLE',
    PENDING = 'PENDING',
    SUCCESS = 'SUCCESS',
    ERROR = 'ERROR',
}

export enum EErrorMessages {
    REQUIRED = 'Bu xana məcburidir.',
}

export enum ECountry {
    TR = 'TR',
    US = 'US',
    CN = 'CN',
}
